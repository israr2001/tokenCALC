# TokenCALC — subdomain app + WordPress blog
# Copy this file to `.env` and replace with your real domain.

# React app (subdomain)
VITE_SITE_URL=https://tools.example.com

# WordPress blog (main domain)
VITE_BLOG_URL=https://example.com
